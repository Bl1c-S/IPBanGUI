<UserControl x:Class="WPF_IPBanUtility.KeyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_IPBanUtility"
             mc:Ignorable="d" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Left">
            <TextBlock Grid.Row="0" Text="Title" FontSize="16" FontWeight="Bold" Margin="5"/>
            
        </DockPanel>

        <TextBox Grid.Row="1" x:Name="EditableTextBox" Text="Initial Text" Margin="5"/>

        <DockPanel  Grid.Row="2" VerticalAlignment="Top" HorizontalAlignment="Right">
            <Button Content="Disable Changed" Margin="5"/>
            <Button Content="Delete Key" Margin="5"/>
        </DockPanel>
    </Grid>
</UserControl>
