<UserControl x:Class="WPF_IPBanUtility.NavigateBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_IPBanUtility"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=local:NavigateBarViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DockPanel VerticalAlignment="Top" HorizontalAlignment="Left">
            <Button Content="Main" Command="{Binding NavigateToKeyList}"/>
            <Button Content="Settings" Command = "{Binding NavigateToSettings}" Margin="8,0,0,0"/>
        </DockPanel>
    </Grid>
</UserControl>
