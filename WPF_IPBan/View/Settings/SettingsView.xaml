<UserControl x:Class="WPF_IPBanUtility.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_IPBanUtility"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <Label Content="Dirrectory path"/>
            <TextBox Text="{Binding DirrectoryPath}"/>
        </StackPanel>

        <Button Content="Save" Command="{Binding SaveChanged}"/>
    </Grid>
</UserControl>
