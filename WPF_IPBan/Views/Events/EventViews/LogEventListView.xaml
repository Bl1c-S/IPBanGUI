<UserControl x:Class="WPF_IPBanUtility.LogEventListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:WPF_IPBanUtility"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" MaxHeight="400">
    <Grid>
        <ui:DataGrid ItemsSource="{Binding LogEvents, Mode=TwoWay}" AutoGenerateColumns="False" BorderThickness="5,1,5,1" CanUserReorderColumns="False">
            <ui:DataGrid.Columns>
                <DataGridTextColumn Header="Номер" Binding="{Binding Id}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat='yyyy-MM-dd HH:mm:ss'}"  IsReadOnly="True"/>
                <DataGridTextColumn Header="Опис" Binding="{Binding Message}"  IsReadOnly="True" Width="*"/>
            </ui:DataGrid.Columns>
        </ui:DataGrid>
    </Grid>
</UserControl>
