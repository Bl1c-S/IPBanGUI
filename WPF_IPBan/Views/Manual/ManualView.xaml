<UserControl x:Class="WPF_IPBanUtility.ManualView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_IPBanUtility"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type local:SupportViewModel}">
                <local:SupportView/>
            </DataTemplate>
        </Grid.Resources>

        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Style="{StaticResource ManualParagraph}" Text="{Binding WelcomeParagraph}"/>
                <TextBlock Style="{StaticResource ManualParagraph}" Text="{Binding Paragraph1}"/>
                <local:SupportView DataContext="{Binding SupportVM}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
